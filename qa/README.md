# QA

Required checks:

- install and typecheck
- Expo web export
- Firebase emulator authentication
- cross-tenant Firestore denial
- campaign create, update, and delete
- failed social publish does not become a successful launch